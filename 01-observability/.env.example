OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o

# Observability — values come from WSO2 Agent Manager after registering
# the agent as an Externally-Hosted Agent. AMP > Settings > API Keys
# issues the JWT used as AMP_AGENT_API_KEY.
AMP_OTEL_ENDPOINT=
AMP_AGENT_API_KEY=
